# AEGIS backend — merged requirements, pinned to Part 2's versions since Moss
# has the tightest constraints. Part 4's own list (pydantic>=2, fastapi,
# unpinned) is compatible with these pins, so no conflict — just standardizing
# on one exact set for a reproducible build across Kunal's/Aditya's machines.

inferedge-moss==1.0.0b8
inferedge-moss-core==0.4.1
pydantic==2.13.5
pydantic_core==2.46.5
fastapi>=0.115
starlette>=0.40
anyio>=4.0
typing_extensions>=4.12
httpx>=0.27

pytest